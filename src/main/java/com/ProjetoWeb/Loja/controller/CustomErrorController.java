/* 

package com.ProjetoWeb.Loja.controller;
import org.springframework.boot.web.servlet.error.ErrorController;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
public class CustomErrorController implements ErrorController {

    @RequestMapping("/error")
    public String handleError() {
        // Aqui você pode redirecionar para uma página de erro personalizada ou manipular de outra forma a resposta de erro
        return "/error.html"; // Substitua "error" pelo nome da página de erro personalizada, se aplicável
    }
}
*/